[package]
name = "bevy_snake"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.12", features = ["dynamic_linking", "jpeg"] }
rand = "0.8.5"
lazy_static = "1.4.0"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
